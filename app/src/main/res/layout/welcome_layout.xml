<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/black"
    >
<!-- H.M.Wang 2023-8-18 将启动页面的两个图片从MainActivity移到WelcomeActivity，用来接收用户的长按事件，目的是备份用户数据后，清空本地数据，以防止在MainActivity启动时不明原因的死机 -->
    <ImageView
        android:id="@+id/image1s"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/load1s" />
    <View
        android:id="@+id/clickView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:background="#01000000" />
<!-- End of H.M.Wang 2023-8-18 将启动页面的两个图片从MainActivity移到WelcomeActivity，用来接收用户的长按事件，目的是备份用户数据后，清空本地数据，以防止在MainActivity启动时不明原因的死机 -->
</RelativeLayout>
